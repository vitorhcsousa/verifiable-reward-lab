# rlvr-from-scratch
From-scratch implementation of the full alignment pipeline: transformer → pretraining → SFT → GRPO → GDPO. Single GPU, small models, one file per concept. The repo is the artifact.
